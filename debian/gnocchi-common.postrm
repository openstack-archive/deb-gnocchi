#!/bin/sh

set -e

if [ "$1" = "purge" ] ; then
	rm -f /etc/gnocchi/gnocchi.conf /etc/gnocchi/policy.json
	rmdir --ignore-fail-on-non-empty /etc/gnocchi || true
	rm -rf /var/lib/gnocchi /var/log/gnocchi
fi

#DEBHELPER#

exit 0
